# Runtime for the Streamlit demo (what Streamlit Community Cloud installs).
# The app only reads the precomputed metrics in results/, so it needs no ML libraries.
# To rerun the benchmark or the tests, install requirements-pipeline.txt instead.
streamlit==1.64.0
altair==6.3.0
pandas==2.3.3
numpy==2.2.6
pyarrow==19.0.1
